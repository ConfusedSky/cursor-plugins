{
  "mcpServers": {
    "docusign": {
      "type": "http",
      "url": "${DOCUSIGN_MCP_URL}",
      "auth": {
        "CLIENT_ID": "${CLIENT_ID}",
        "CLIENT_SECRET": "${CLIENT_SECRET}"
      }
    }
  }
}
